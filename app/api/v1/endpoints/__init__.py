# backend/app/api/v1/endpoints/__init__.py
# This file marks the 'endpoints' directory as a Python package.
# Submodules should be imported directly from their specific paths,
# not exposed at this package level, to avoid circular dependencies.